<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:micro="clr-namespace:Microcharts.Maui;assembly=Microcharts.Maui"
             x:Class="MauiStrides.Views.SummaryPage"
             Title="Statistics">
    
    <ScrollView>
        <VerticalStackLayout Spacing="20" Padding="20">
            
            <!-- Chart 1: Distance -->
            <Frame>
                <VerticalStackLayout>
                    <Label Text="Total Distance per Sport" 
                           FontSize="Title" 
                           FontAttributes="Bold" 
                           HorizontalOptions="Center"/>
                    <micro:ChartView Chart="{Binding DistanceChart}" 
                                     HeightRequest="500"/>
                </VerticalStackLayout>
            </Frame>

          

              <!--- Utkommenterad medans buggsök-->
            <!--<Frame>
                <VerticalStackLayout>
                    <Label Text="Activities per Month" 
                           FontSize="Title" 
                           FontAttributes="Bold" 
                           HorizontalOptions="Center"/>
                    <micro:ChartView Chart="{Binding ActivityCountChart}" 
                                     HeightRequest="300"/>
                </VerticalStackLayout>
            </Frame>-->

        </VerticalStackLayout>
    </ScrollView>
    
</ContentPage>